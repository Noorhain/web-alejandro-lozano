import { SaneadorHtmlPipe } from './saneador-html.pipe';

describe('SaneadorHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new SaneadorHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
